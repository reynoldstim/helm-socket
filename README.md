# Helm Socket

A WebSocket-based control plane for managing secure, multi-tenant interactive access to remote systems such as SSH-accessible hosts, Kubernetes clusters, and other execution environments.

The service is responsible for authentication, authorization, session lifecycle management, and tenant isolation, while delegating execution to pluggable backend agents.
